/**
 * Глобальные константы баланса игры v0.3.6
 * Централизованное управление всеми параметрами баланса
 */

export const GLOBAL_BALANCE_CONFIG = {
    // ============================================
    // БАЗОВЫЕ ПАРАМЕТРЫ БАЛАНСА
    // ============================================

    /** Множитель сложности - влияет на HP врагов, урон, цены */
    K_DIFF: 1.0,

    /** Маржа рынка - отношение цены продажи к цене покупки */
    K_ECONOMY: 0.8,

    /** Экспонента прогрессии - крутизна роста характеристик от Tier к Tier */
    ALPHA_PROG: 1.25,

    // ============================================
    // PRODUCTION & CRAFTING
    // ============================================

    /** Базовое время крафта для Tier 1 (секунды) */
    BASE_CRAFT_TIME: 60,

    /** Шаг глубины для масштабирования характеристик (метры) */
    DEPTH_UNIT: 5000,

    // ============================================
    // GLOBAL MAP PHYSICS
    // ============================================

    /** Показатель степени для расхода топлива (квадратичная зависимость от массы) */
    MASS_PENALTY: 2.0,

    /** Фактор снижения скорости от перегруза (откалиброванный для таблицы зависимостей) */
    SPEED_PENALTY_FACTOR: 0.5,

    /** Влияние массы на вероятность инцидентов */
    K_WEIGHT_INCIDENT: 0.3,

    /** Максимальная вероятность инцидента (75%) */
    MAX_INCIDENT_CHANCE: 0.75,

    // ============================================
    // EQUIPMENT MASS PARAMETERS
    // ============================================

    /** Базовая масса Hull Tier 1 (кг) */
    HULL_MASS_BASE: 500,

    /** Показатель роста массы Hull (< 1 = замедленный рост) */
    HULL_MASS_EXPONENT: 0.875,

    /** Базовые массы для остальных типов equipment Tier 1 (кг) */
    BIT_MASS_BASE: 10,
    ENGINE_MASS_BASE: 80,
    COOLER_MASS_BASE: 30,
    LOGIC_MASS_BASE: 5,
    CONTROL_MASS_BASE: 8,
    GEARBOX_MASS_BASE: 50,
    POWER_MASS_BASE: 100,
    ARMOR_MASS_BASE: 150,
    CARGO_BAY_MASS_BASE: 200,

    // ============================================
    // FUEL EFFICIENCY
    // ============================================

    /** Дальность на 1 единицу топлива (км/ед) */
    FUEL_EFFICIENCY: {
        coal: 50,    // км на 1 уголь
        oil: 200,    // км на 1 нефть
        gas: 500     // км на 1 газ
    },

    // ============================================
    // REGIONAL MODIFIERS (GLOBAL MAP)
    // ============================================

    /** Региональные модификаторы сопротивления (влияет на расход топлива) */
    REGION_DRAG: {
        rust_valley: 1.0,
        crystal_wastes: 1.3,
        iron_gates: 1.5,
        magma_core: 2.0,
        void_chasm: 2.5
    },

    /** Базовая вероятность инцидентов по регионам */
    REGION_BASE_INCIDENT: {
        rust_valley: 0.05,     // 5% базовый риск
        crystal_wastes: 0.15,  // 15%
        iron_gates: 0.20,      // 20%
        magma_core: 0.25,      // 25%
        void_chasm: 0.30       // 30%
    },

    // ============================================
    // TRAVEL MECHANICS
    // ============================================

    /** Минимальная скорость путешествия (% от базовой) */
    MIN_TRAVEL_SPEED_PERCENT: 0.2,  // 20% минимум

    /** Коэффициент конвекции для охлаждения в пути (показатель степени) */
    CONVECTION_FACTOR: 2.0  // Квадратичная зависимость от скорости

} as const;

// Типы для региональных ID
export type RegionId = keyof typeof GLOBAL_BALANCE_CONFIG.REGION_DRAG;
export type FuelType = keyof typeof GLOBAL_BALANCE_CONFIG.FUEL_EFFICIENCY;
