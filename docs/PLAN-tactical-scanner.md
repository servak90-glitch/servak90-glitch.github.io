# 📝 План: Lithosphere Tactical Scanner (Вертикальная Навигация)

Этот план описывает процесс объединения вкладок Города, Кузни и Карты в единую интерактивную систему «Тактического Сканера» с вертикальной прогрессией.

## 🔴 Анализ и Цели
1.  **Навигация**: Полный отказ от разрозненных вкладок `CityView` и `ForgeView`.
2.  **Глубина**: Визуализация игрового мира как вертикального разреза литосферы.
3.  **Tier-Lock**: Ограничение тира крафта в Кузне в зависимости от текущего региона (3 тира на регион).
4.  **UX**: Снижение когнитивной нагрузки за счет единого контекста "Карта = Всё управление".

---

## 🛠️ Фаза 1: Реструктуризация Стора и Навигации
- [ ] **types.ts**: Добавить `tierLimit` и `availableModules` в конфиг регионов.
- [ ] **NavigationManager.tsx**: Удалить ссылки на `View.CITY` и `View.FORGE`.
- [ ] **App.tsx**: Обновить переключатель видов (оставить только Drill, Map, Codex).

---

## 🛰️ Фаза 2: Новый интерфейс Карты (Vertical Schematic)
- [ ] **ScannerCanvas.tsx**: Создать новый компонент вместо `IsometricCanvas`.
    - Вертикальный скролл с использованием `Framer Motion`.
    - Отрисовка регионов как цветовых зон с неоновыми границами.
    - Визуализация городов как узлов (Nodes) с анимацией "пульсации".
- [ ] **NodeInteraction**: Реализовать масштабирование (Zoom-in) при клике на узел города.

---

## ⚒️ Фаза 3: Региональный Хаб (Unified Overlay)
- [ ] **RegionalHubOverlay.tsx**: Создать компонент, открывающийся при подключении к городу.
    - Вкладка **[MARKET]**: Стандартная торговля с учетом локальных цен.
    - Вкладка **[FORGE]**: Проверка региональной лицензии. Если Tier предмета > Tier региона — блокировка.
    - Вкладка **[DRONES]**: Управление экспедициями в текущем регионе.

---

## 🎨 Фаза 4: UI/UX Pro Max (Styling)
Стиль: **Tactical Dashboard / Data Visualization**

### Цветовая палитра (Dashboard Core)
- **Background**: `#0F172A` (Slate 900) - Основной фон бездны.
- **Primary**: `#38BDF8` (Sky 400) - Акценты сканера, линии связи.
- **Accent**: `#F472B6` (Pink 400) - Критические зоны, точки боссов.
- **Success**: `#4ADE80` (Green 400) - Доступные города, успешный крафт.
- **Warning**: `#FB923C` (Orange 400) - Требуемый тир, перегрев.

### Типографика
- **Headers/Display**: `Fira Sans` - для названий регионов и городов.
- **Monospace**: `Fira Code` - для всех технических данных, тиров и системных логов.

### Визуальные эффекты
- **Glow**: `text-shadow: 0 0 10px rgba(56, 189, 248, 0.5)` для элементов сканера.
- **Glassmorphism**: Панели Хаба с `backdrop-blur-xl` и прозрачностью `bg-slate-900/80`.
- **Transitions**: Плавные переходы (150-300ms) для всех ховер-состояний.

---

## ✅ Чек-лист верификации
- [ ] Игрок не может скрафтить Tier 4 в первом регионе.
- [ ] Перемещение между городами отображается на вертикальной шкале.
- [ ] Карта плавно скроллится на мобильных устройствах.
- [ ] Все иконки — SVG (Lucide), никаких эмодзи в интерфейсе сканера.

---

## 🚀 Следующие шаги
1. Подготовить `ScannerCanvas.tsx` с базовой разметкой слоев.
2. Перенести логику Кузни в новый `RegionalHubOverlay`.
